language: php
os: linux
dist: jammy

php:
  - 8.5

before_install:
  - echo "Checking if PHP binary is available..."

script:
  - which php || echo "PHP binary not found"
  - php -v || echo "PHP 8.5 is not available"
