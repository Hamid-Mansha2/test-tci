language: php

os: linux
dist: jammy
sudo: required
group: stable

php:
  - '8.2'

addons:
  apt:
    update: true
    packages:
      - libonig-dev
      - openjdk-17-jdk

sonarcloud:
  organization: "eagle-eye-solutions"

script:
  - php -v
  - java -version
  - sonar-scanner -v
