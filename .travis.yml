language: php

os: linux
dist: focal
sudo: required
group: stable

php:
  - '8.2.13'

addons:
  apt:
    update: true
    packages:
      - libonig-dev
      - openjdk-17-jdk
      - openjdk-17-jre

script:
  - php -v
  - java -version
